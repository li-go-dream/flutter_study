import 'package:flutter/material.dart';

class BdPage extends StatelessWidget {
  const BdPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('bd'));
  }
}
