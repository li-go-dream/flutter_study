import 'package:flutter/material.dart';

class BdWorkBenchPage extends StatelessWidget {
  const BdWorkBenchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('bdwoekbench'));
  }
}
